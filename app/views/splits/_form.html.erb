<% semantic_form_for([@profile, @split]) do |f| %>
  <%= f.error_messages %>
  <%= f.inputs :path %>
  <%= f.input :depth, :collection => [1,2], :as => :select %>
  <button type='submit'>Save</button>
<% end %>
