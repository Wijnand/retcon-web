<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
<head>
	<meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>

	<title>Retcon: <%= yield_or_default yield(:header) %> </title>
	  <%= javascript_include_tag "top_up-min.js", "jquery.quicksearch",  "jquery.tablesorter.min", "application" %>
    <%= stylesheet_link_tag 'screen', 'formtastic', 'formtastic_changes',  :cache => true %>
</head>

<body>

  <div id="header">
    <div id="global_links">
      <%= render :partial => 'shared/global_links' %>
    </div>
    <h1><%= link_to ('Retcon <span>blast from the past</span>', root_path) %></h1>

    <div id="tabs">
      <%= render :partial => 'shared/tabs' %>
    </div>
  </div>

  <div id="main">

    <div id="left">
    <% flash.keys.each do | key | %>
       <div id="<%= key %>"><%= flash[key] %></div>
    <% end %>
    <% build_action_list %>
      <div class="head"><h2><%= yield_or_default yield(:header), "Use 'content_for :header do; end' in your view to set this title" %></h2>
        <%= yield_or_default(yield(:sidebar)) %>
        <%= yield(:search) %>
      </div>
      <div id="screen">
        <%= yield %>
      </div>
    </div>
  </div>
  <div id="footer">
    <a href="http://github.com/wijnand/retcon-web">Retcon project</a>
  </div>


</body>
</html>
