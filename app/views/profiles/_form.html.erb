<% semantic_form_for(@profile) do |f| %>
  <%= f.error_messages %>
  <% f.inputs do %>
    <%= f.input :name %>
    <%= f.input :servers, :collection => Server.find(:all, :order => 'hostname ASC'), :as => :check_boxes, :label => 'Apply to', :hint => 'Quickly apply this profile to all these servers' %>
  <% end %>
  <%= f.buttons %>
<% end %>